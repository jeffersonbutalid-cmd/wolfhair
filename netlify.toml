# Wolf Hair Restoration — Netlify configuration
# Static site: no build step. The repo root is published as-is.

[build]
  publish = "."
  command = ""

# Serverless functions (chat assistant). Netlify installs deps from package.json.
[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# Friendly path for the chat function (same-origin rewrite, no redirect).
[[redirects]]
  from = "/api/chat"
  to = "/.netlify/functions/chat"
  status = 200

# Server-side Google Ads offline conversion upload (gclid-only). GHL posts here.
[[redirects]]
  from = "/api/conversion"
  to = "/.netlify/functions/gads-conversion"
  status = 200

# Clean URLs: serve /page instead of /page.html and drop the extension.
[[redirects]]
  from = "/index.html"
  to = "/"
  status = 301
  force = true

# Security headers on every response.
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "geolocation=(), microphone=(), camera=()"
    Strict-Transport-Security = "max-age=31536000; includeSubDomains; preload"

# Long-cache images (rarely change).
[[headers]]
  for = "/assets/img/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# CSS/JS use fixed filenames, so revalidate to pick up edits on each deploy.
[[headers]]
  for = "/assets/*.css"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"

[[headers]]
  for = "/assets/*.js"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"

# Keep HTML fresh so content edits go live immediately.
[[headers]]
  for = "/*.html"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"
